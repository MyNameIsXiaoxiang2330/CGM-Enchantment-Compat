# 与官方 CGM 的附魔差异分析

> 本文档详细解释本模组与官方高版本 CGM (1.0.1+) 附魔系统的差异、原因，以及未实现/有 Bug 功能的说明。
> 最后更新: 2026-07-07

---

## 为什么有差异？

本模组的目标平台是 **Minecraft 1.12.2 + CGM v0.15.3**。高版本 CGM (1.0.1+) 经历了大规模重写：
- 从 `Forge` 迁移到 `Framework`（自研加载器）
- 引入数据驱动系统（JSON 配置 → 数据包）
- 重写了实体碰撞、弹药系统、附魔系统
- 引入了暴击、配件系统等新机制

**CGM v0.15.3 没有这些机制**，所以我们只能在现有架构下用不同的方式实现"类似"的效果。

---

## 附魔逐一对比

### 1. 穿甲弹 (Puncturing)

| 项目 | 官方 CGM 1.0.1+ | 本模组 |
|------|-----------------|--------|
| 效果 | 暴击率 +5%/级 | **忽视护甲 25%/级** |
| 最高等级 | IV | IV |
| 是否一致 | — | ❌ **完全不同** |

**为什么不一样？**

CGM v0.15.3 **没有暴击系统**。暴击机制是 CGM 1.0+ 版本才引入的，它依赖于 Framework 的事件系统和数据驱动中的 `damage` 计算链。在 1.12.2 的 CGM 中，子弹的伤害计算是硬编码在 `EntityBullet` 中的，无法在不修改字节码的情况下插入暴击判定。

**我们的替代方案**：利用原版 Minecraft 的护甲机制，直接忽视目标护甲值。这不需要修改 CGM 的伤害计算链，只需在 `LivingHurtEvent` 中调整最终伤害即可实现。

---

### 2. 间接伤害 (Collateral)

| 项目 | 官方 CGM 1.0.1+ | 本模组 |
|------|-----------------|--------|
| 效果 | 子弹穿透实体，伤害不减 | 子弹穿透实体，伤害不减 |
| 最高等级 | **I** | **III** |
| 粒子效果 | 无 | **白色烟花粒子** |
| 是否一致 | — | ⬆️ **增强版** |

**为什么不同？**

官方 CGM 将间接伤害锁定为 I 级（只有穿透，不缩放）。我们增加了 II~III 级（每级伤害加成 +20%），这是因为：
- 我们的穿透系统不修改 CGM 的实体碰撞逻辑，而是通过 `EntityJoinWorldEvent` 拦截子弹后再用**独立射线**进行穿透检测
- 独立射线允许我们自由控制伤害缩放
- 粒子效果（`FIREWORKS_SPARK`）是额外的视觉反馈

**实现原理**：
```
子弹生成 → BulletHandler 捕获 → 记录射击者+方向+伤害
         → 丢弃原始子弹（CollateralHandler 接管）
         → 每 tick 推进射线 1.5 格
         → AABB 碰撞检测 → 命中实体 → 造成伤害 → 继续穿透
```

---

### 3. 快速扳机 (Trigger Finger)

| 项目 | 官方 CGM 1.0.1+ | 本模组 |
|------|-----------------|--------|
| 状态 | ✅ 正常 | ❌ **WIP — 未实现** |
| 原因 | — | CGM v0.15.3 冷却系统不可拦截 |

**为什么做不了？**

CGM v0.15.3 的射速由枪械 JSON 中的 `general.rate` 字段控制，该值在类加载时从 JSON 解析并缓存到 `Gun` 对象中。开火判定流程如下：

```
1. 玩家扣动扳机
2. CGM 检查 CommonEvents.getCooldownTracker(UUID) 的剩余冷却
3. 如果冷却 > 0 → 不开火
4. 如果冷却 ≤ 0 → 开火，重置冷却 = rate
```

**问题**：冷却值在 CGM 内部方法中被直接读取和比较，我们无法通过：
- **反射**：无法在运行时修改私有字段的访问路径
- **CoreMod ASM**：可以注入，但冷却检查在多个方法中硬编码，需要修改 4+ 个方法，冲突风险极高
- **事件监听**：Forge 没有"冷却即将检查"的事件

**替代方案**（未来考虑）：
- 直接 ASM 修改 `rate` 的读取路径（高风险）
- 在 CGM 的冷却检查点注入自定义 Hook

目前从附魔台移除，保留注册以防存档损坏。

---

### 4. 弹药回收 (Reclaimed)

| 项目 | 官方 CGM 1.0.1+ | 本模组 |
|------|-----------------|--------|
| 效果 | 概率返还弹药 | 33%/50%/87.5% 概率返还 |
| 最高等级 | III | III |
| Bug | 无 | ⚠️ **退弹时也触发返还** |

**Bug 描述**：按下退弹键（R）时，弹药回收的返还逻辑会错误触发，导致退弹后子弹不减反增。

**为什么修不好？**

核心问题：**本模组无法区分"弹药消耗来自开火"还是"弹药消耗来自退弹"**。

我们尝试了 5 种方案，全部失败：

| 方案 | 做法 | 失败原因 |
|------|------|---------|
| ① 阈值判断 | 弹药减少量 > 8 视为退弹 | CGM 退弹是逐 tick 减 1 发，不是一次清空 |
| ② 子弹生成标记 | BulletHandler 检测到子弹生成时设标记 | `findShooter()` 反射在某些场景返回 null |
| ③ `isHandActive()` | 开枪时玩家按着右键 | CGM 未对所有武器设置手部活动状态 |
| ④ CGM 冷却反射 | 检测 `CommonEvents.getCooldownTracker` | CGM 的 CooldownTracker API 无法通过反射获取 |
| ⑤ 世界 tick 追踪 | 记录子弹生成时的世界 tick 对比 | `EntityJoinWorldEvent` 触发时序与预期不符 |

**根源**：CGM v0.15.3 的弹药管理是通过 `DataParameter`（网络同步数据）+ `NBTTagCompound`（持久化）双轨实现的。退弹和开火最终都调用同一套 NBT 写入代码，从外部无法区分调用来源。

**替代方案**（规划中）：
- 在 `ItemGun` 的开火方法中 ASM 注入一个标记位
- 或者：监听 CGM 的网络包，区分"开火包"和"退弹包"
- 低优先级方案：退弹后检查玩家是否在短时间内再次开枪，如果是则视为退弹结束（启发式）

---

### 5. 附魔冲突系统

| 项目 | 官方 CGM 1.0.1+ | 本模组 |
|------|-----------------|--------|
| 冲突规则 | **O/X 兼容矩阵**（9×9） | **按 EnchantmentType 分类**（3 类） |
| 精度 | 每对附魔独立指定 | 同类型全冲突，不同类型全兼容 |

**官方兼容表**（O=可共存，X=冲突）：

| | 快速扳机 | 弹药回收 | 超容量 | 轻装上阵 | 间接伤害 | 加速器 | 穿甲弹 | 熟练手 | 纵火者 |
|---|---|---|---|---|---|---|---|---|---|
| 快速扳机 | O | O | **X** | **X** | O | O | O | O | O |
| 弹药回收 | O | O | O | O | O | O | O | **X** | O |
| 超容量 | **X** | O | O | **X** | O | O | O | O | O |
| 轻装上阵 | **X** | O | **X** | O | O | O | O | O | O |
| 间接伤害 | O | O | O | O | O | **X** | **X** | O | **X** |
| 加速器 | O | O | O | O | **X** | O | **X** | O | **X** |
| 穿甲弹 | O | O | O | O | **X** | **X** | O | O | **X** |
| 熟练手 | O | **X** | O | O | O | O | O | O | O |
| 纵火者 | O | O | O | O | **X** | **X** | **X** | O | O |

**我们的当前行为**（按类型 WEAPON/AMMO/PROJECTILE）：

| 附魔 | 类型 | 冲突的附魔 |
|------|------|-----------|
| 快速扳机 | WEAPON | 熟练手、轻装上阵 |
| 熟练手 | WEAPON | 快速扳机、轻装上阵 |
| 轻装上阵 | WEAPON | 快速扳机、熟练手 |
| 超容量 | AMMO | 弹药回收 |
| 弹药回收 | AMMO | 超容量 |
| 加速器 | PROJECTILE | 穿甲弹、间接伤害、纵火者、弧光引导、高爆弹、FELLBULLET 系列 |
| 穿甲弹 | PROJECTILE | 同上 |
| 间接伤害 | PROJECTILE | 同上 |
| 纵火者 | PROJECTILE | 同上 |
| 弧光引导 | PROJECTILE | 同上 |
| 高爆弹 | PROJECTILE | 同上 |
| FELLBULLET | PROJECTILE | 同上 |

**差异**：
- ✅ 间接伤害 X 加速器/穿甲弹/纵火者 — 与官方一致（同类冲突）
- ❌ **超容量 X 快速扳机** — 官方冲突，我们**不冲突**
- ❌ **弹药回收 X 熟练手** — 官方冲突，我们**不冲突**
- ❌ **轻装上阵 X 超容量** — 官方冲突，我们**不冲突**

**计划**：未来将 9 个标准附魔改为精确的 O/X 矩阵实现。

---

## 快速参考表

| 附魔 | 官方 CGM | 本模组 | 一致？ |
|------|---------|--------|-------|
| 加速器 | 弹速+15%, 伤害+10%/级, II级 | 同官方 | ✅ |
| 穿甲弹 | **暴击率+5%/级** | **护甲穿透25%/级** | ❌ 见 §1 |
| 间接伤害 | 穿透, I级 | 穿透, **III级**, **粒子** | ⬆️ 见 §2 |
| 纵火者 | 点燃, I级 | 同官方 | ✅ |
| 弧光引导 | **不存在** | 原创附魔 | 🆕 |
| 高爆弹 | **不存在** | 原创附魔 | 🆕 |
| 轻装上阵 | 开镜加速, I级 | 同官方 | ✅ |
| 超容量 | 弹匣+50%/级, III级 | 同官方 | ✅ |
| 熟练手 | 装弹-3tick/级, II级 | 同官方 | ✅ |
| 弹药回收 | 概率返还, III级 | 同官方, **有退弹 Bug** | ⚠️ 见 §4 |
| 快速扳机 | 提高射速 | **WIP 未实现** | ❌ 见 §3 |
| FELLBULLET | **不存在** | EGO 还原 | 🆕 |
| FELLBULLET Piercer | **不存在** | EGO 还原 | 🆕 |

---

## 技术实现原理

### CoreMod 注入点

本模组的 CoreMod 在类加载时修改 CGM 的两个类：

```
GunTransformer (IFMLLoadingPlugin)
  └─ GunClassTransformer (IClassTransformer)
       ├─ ItemGun → 注入 getItemEnchantability() → 返回 30
       └─ ReloadTracker.isWeaponFull() → 替换弹容量读取路径
```

### 事件驱动架构

所有附魔效果通过 Forge 事件总线实现，不修改任何 CGM 字节码（除上述两处注入外）：

| 事件 | 处理器 | 处理附魔 |
|------|--------|---------|
| `LivingDeathEvent` | SinBulletHandler | FELLBULLET 系列 |
| `LivingHurtEvent` | DamageHandler | 加速器、穿甲弹、纵火者、弧光引导、高爆弹 |
| `LivingHurtEvent` | HighExplosiveHandler | 高爆弹（额外爆炸） |
| `EntityJoinWorldEvent` | BulletHandler | 加速器（弹速）、纵火者（追踪）、高爆弹（霰弹射程）|
| `EntityJoinWorldEvent` | CollateralHandler | 间接伤害（穿透） |
| `EntityJoinWorldEvent` | HighExplosiveHandler | 高爆弹（火箭追踪） |
| `PlayerTickEvent` | GunStateHandler | 超容量、弹药回收、熟练手 |
| `PlayerTickEvent` | PlayerHandler | 轻装上阵（移速+散布） |
| `ExplosionEvent.Detonate` | ArcLightHandler | 弧光引导（爆炸AOE） |
| `WorldTickEvent` | 多个 Handler | 子弹追踪、射线推进、蓄力动画 |

### 为什么不用 Mixin？

见 [`COREMOD.md`](COREMOD.md) 第 4 节中的详细说明。
